import { Navigation } from '../Navigation/Navigation';
import { AppBarHeader } from './AppBar.styled';

// =========================================

export const AppBar = () => {
  return (
    <AppBarHeader>
      <Navigation />
    </AppBarHeader>
  );
};
